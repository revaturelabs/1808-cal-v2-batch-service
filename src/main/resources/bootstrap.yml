spring:
  application:
    name: batch-service
  cloud:
    config:
      name: batch-service
      discovery:
        service-id: config
        enabled: true
        
server:
  servlet-path: /batch/*
---
spring:
  profiles: local

---
spring:
  profiles: dev

eureka:
  client:
    service-url:
      defaultZone: http://caliber-2-dev-alb-315997072.us-east-1.elb.amazonaws.com/eureka
---
spring:
  profiles: prod
  